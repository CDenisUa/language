/** Filled in after `wrangler deploy` (worker/) and VAPID key generation — see DECISIONS.md. */
export const PUSH_WORKER_URL = 'REPLACE_AFTER_WORKER_DEPLOY'
export const VAPID_PUBLIC_KEY = 'REPLACE_AFTER_VAPID_KEYGEN'
