/** How far ahead (in minutes) a labeled schedule occurrence triggers an in-app reminder banner. */
export const UPCOMING_EVENT_LEAD_MINUTES = 60

/** How often the reminder clock re-evaluates while the app is open. */
export const REMINDER_TICK_INTERVAL_MS = 60_000

export const DISMISSED_REMINDERS_STORAGE_KEY = 'sprachlabor-dismissed-reminders'
