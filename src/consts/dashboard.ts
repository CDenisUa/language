/** Sessions shorter than this are treated as an accidental page visit, not real study time. */
export const MIN_STUDY_SESSION_SECONDS = 5

/** Default target share of study time for German, per DECISIONS.md — English is the remainder. */
export const DEFAULT_TARGET_GERMAN_PERCENT = 70
